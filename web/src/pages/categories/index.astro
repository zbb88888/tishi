---
/**
 * /categories — 12 个 AI 分类浏览
 *
 * 数据源：data/categories.json
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCategories } from '../../lib/data';

const categories = getCategories().sort((a, b) => a.sort_order - b.sort_order);
---

<BaseLayout title="项目分类">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">📂 项目分类</h1>
    <p class="mt-2 text-gray-600">按 AI 子领域浏览热门开源项目</p>
  </div>

  {categories.length === 0 ? (
    <div class="card text-center text-gray-500 py-12">
      <p>暂无分类数据</p>
    </div>
  ) : (
    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      {categories.map(cat => (
        <a href={`/categories/${cat.slug}`} class="card block group">
          <h3 class="text-lg font-semibold text-gray-900 group-hover:text-primary-600 transition-colors">
            {cat.name}
          </h3>
          <p class="mt-1 text-sm text-gray-500">{cat.description}</p>
          <div class="mt-4 flex items-center justify-between">
            <span class="badge-blue">{cat.project_ids.length} 个项目</span>
            <span class="text-xs text-gray-400">{cat.slug}</span>
          </div>
        </a>
      ))}
    </div>
  )}
</BaseLayout>
