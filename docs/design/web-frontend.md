# 前端展示模块设计（Web Frontend）

## 概述

tishi 前端使用 Astro 构建，采用 SSG（Static Site Generation）模式。在构建时调用 API Server 获取数据，生成纯静态 HTML 页面，由 Nginx 托管。

## 页面结构

```
/                          ← 首页：排行榜 + 概览
/rankings                  ← 完整排行榜（分页）
/projects/:full_name       ← 项目详情 + 趋势图表
/blog                      ← 博客文章列表
/blog/:slug                ← 博客文章详情
/categories                ← 分类浏览
/categories/:slug          ← 分类下项目列表
/about                     ← 关于页面
/rss.xml                   ← RSS Feed
```

## 页面设计

### 首页 (`/`)

```
┌─────────────────────────────────────────────────┐
│  🔥 tishi — AI 开源趋势追踪                      │
│                                                  │
│  今日更新: 2026-02-12 | 追踪项目: 100            │
├─────────────────────────────────────────────────┤
│                                                  │
│  📊 Top 10 排行榜                                │
│  ┌────┬──────────────┬────────┬────────────┐    │
│  │排名│ 项目          │ Star   │ 日增 Star  │    │
│  ├────┼──────────────┼────────┼────────────┤    │
│  │ 1  │ langchain    │ 95.2k  │ +523  ↑2   │    │
│  │ 2  │ ollama       │ 88.1k  │ +412  ↓1   │    │
│  │... │              │        │            │    │
│  └────┴──────────────┴────────┴────────────┘    │
│                                                  │
│  [查看完整排行榜 →]                               │
│                                                  │
├─────────────────────────────────────────────────┤
│                                                  │
│  🆕 本周新入榜                                    │
│  ┌─────────────────────────────────────────┐    │
│  │ project-name  ⭐ 12.3k  📝 Python      │    │
│  │ 项目描述...                              │    │
│  └─────────────────────────────────────────┘    │
│                                                  │
├─────────────────────────────────────────────────┤
│                                                  │
│  📝 最新博客                                     │
│  • AI 开源周报 #07 | 2026-02-08                 │
│  • 新项目速递 | example/ai-project               │
│                                                  │
└─────────────────────────────────────────────────┘
```

### 项目详情页 (`/projects/:full_name`)

```
┌─────────────────────────────────────────────────┐
│  langchain-ai/langchain                          │
│  🏷️ LLM | Agent | Framework                     │
│                                                  │
│  Build context-aware reasoning applications      │
│                                                  │
│  ⭐ 95,234  🍴 15,432  📝 Python  📄 MIT       │
│  🔥 热度评分: 92.5/100  | 排名: #1              │
│                                                  │
├─────────────────────────────────────────────────┤
│                                                  │
│  📈 Star 增长趋势（30天/90天/1年）                │
│  ┌─────────────────────────────────────────┐    │
│  │         ╱──╲                            │    │
│  │   ╱────╱    ╲───╱╲                     │    │
│  │ ─╱                  ╲───────            │    │
│  │                                         │    │
│  └─────────────────────────────────────────┘    │
│                                                  │
│  📊 指标对比                                     │
│  │ 日增 Star: +523                              │
│  │ 周增 Star: +3,245                            │
│  │ Fork 率: 16.2%                               │
│  │ Issue 活跃度: 高                              │
│                                                  │
└─────────────────────────────────────────────────┘
```

## 技术架构

### Astro 项目结构

```
web/
├── astro.config.mjs
├── package.json
├── tsconfig.json
├── public/
│   ├── favicon.svg
│   └── robots.txt
├── src/
│   ├── components/
│   │   ├── Header.astro
│   │   ├── Footer.astro
│   │   ├── RankingTable.astro
│   │   ├── ProjectCard.astro
│   │   ├── TrendChart.tsx       ← React Island（交互式图表）
│   │   ├── CategoryBadge.astro
│   │   └── BlogPostCard.astro
│   ├── layouts/
│   │   ├── BaseLayout.astro
│   │   └── BlogLayout.astro
│   ├── pages/
│   │   ├── index.astro
│   │   ├── rankings.astro
│   │   ├── projects/
│   │   │   └── [fullName].astro
│   │   ├── blog/
│   │   │   ├── index.astro
│   │   │   └── [slug].astro
│   │   ├── categories/
│   │   │   ├── index.astro
│   │   │   └── [slug].astro
│   │   ├── about.astro
│   │   └── rss.xml.ts
│   ├── lib/
│   │   └── api.ts              ← API 客户端（构建时调用）
│   └── styles/
│       └── global.css
└── dist/                        ← 构建产物
```

### Islands Architecture

Astro 默认不发送 JS 到客户端。仅交互式组件（图表）使用 React/Preact Island：

```astro
---
// projects/[fullName].astro
import TrendChart from '../../components/TrendChart.tsx';
const { trends } = await fetchProjectTrends(fullName);
---

<BaseLayout>
  <!-- 静态内容，零 JS -->
  <h1>{project.fullName}</h1>
  <p>{project.description}</p>

  <!-- 交互式图表，按需加载 JS -->
  <TrendChart client:visible data={trends} />
</BaseLayout>
```

### 图表库

使用 **Chart.js**（轻量，Astro Island 按需加载）：

- Star 增长折线图
- 分类占比饼图
- 排名变动柱状图

### SSG 数据获取

```typescript
// src/lib/api.ts
const API_BASE = import.meta.env.API_URL || 'http://localhost:8080';

export async function fetchRankings(page = 1, perPage = 20) {
    const res = await fetch(`${API_BASE}/api/v1/rankings?page=${page}&per_page=${perPage}`);
    return res.json();
}

export async function fetchProject(id: string) {
    const res = await fetch(`${API_BASE}/api/v1/projects/${id}`);
    return res.json();
}

export async function fetchProjectTrends(id: string, days = 30) {
    const res = await fetch(`${API_BASE}/api/v1/projects/${id}/trends?days=${days}`);
    return res.json();
}
```

## 样式方案

- **CSS 框架**：Tailwind CSS（Astro 官方集成，原子化 CSS，构建时 purge）
- **色彩主题**：暗色主题为主（开发者偏好），支持明暗切换
- **响应式**：移动端优先，支持桌面/平板/手机

## SEO

- 每个页面有独立 `<title>` 和 `<meta description>`
- 结构化数据（JSON-LD）
- `sitemap.xml` 自动生成
- `robots.txt` 配置
- RSS Feed（`/rss.xml`）

## 构建 & 部署

```bash
cd web
npm install
npm run build     # 输出到 dist/
```

构建产物直接挂载到 Nginx 容器的 `/usr/share/nginx/html`。

## 相关文档

- [API 服务](api-server.md) — 数据来源
- [内容生成](content-generator.md) — 博客内容来源
- [部署拓扑](../architecture/deployment-topology.md) — Nginx 托管
